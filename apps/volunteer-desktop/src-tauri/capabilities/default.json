{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window's web view may ask the host for. Everything else the app does (notifications, autostart, the updater, window show/hide, restarting after an update) runs on the Rust side, which needs no permission here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
